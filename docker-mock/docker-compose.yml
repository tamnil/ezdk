version: "3"
services:
  node:
    build:
      context: ./docker/node
      dockerfile: Dockerfile
    ports:
      - "8880:8880"
      # - "9000:9000"
      # - "443:443"
    volumes:
      - ./app:/var/www/app
    command: npm start
    # links:
      # - app
    # restart: always

